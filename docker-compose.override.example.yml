# Copy to docker-compose.override.yml for local development.
# Easypanel injects its own override with proxy routing — do not use this file there.
services:
  evolution-api:
    ports:
      - "${EVOLUTION_PORT:-8080}:8080"
